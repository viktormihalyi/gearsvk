# Building

Dependencies:

* Vulkan SDK: https://vulkan.lunarg.com/sdk/home
* Python 3 (64 bit)
* conan (`pip install conan`)

```
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .
```
